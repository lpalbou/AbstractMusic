"""Internal Stable Audio 3 inference subset.

This package contains the minimal MIT-licensed model components needed by
AbstractMusic's Stable Audio 3 backend. It intentionally does not expose the
upstream CLI, UI, training, or LoRA workflows as public AbstractMusic APIs.
"""
